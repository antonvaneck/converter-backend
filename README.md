# Unit Converter

Build Spring Boot application
```
mvn clean install
```

Build Docker image
```
docker build -t <image name> .
```

Run Docker image
```
docker run -p8080:8080 <image_name> -it
```

